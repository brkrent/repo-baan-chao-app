-- ==========================================================
-- Addon 6 — รันไฟล์นี้ "ต่อจาก" rental-app-schema-addon-5.sql เดิม
-- เพิ่มวันครบกำหนดชำระต่อรอบบิล (แก้ไขได้ทีหลังโดยเจ้าของบ้าน)
-- ==========================================================

alter table billing_cycles add column if not exists due_date date;
